# ============================================================
# requirements.txt — NBO 2.0 Backend
# Actualizado: agosto 2026
# Python recomendado: 3.12 o 3.13 (3.11 ya no soportado por NumPy 2.5+)
# ============================================================

# --- Framework web ---
fastapi[standard]>=0.124.0,<0.125.0
uvicorn[standard]>=0.34.0,<0.35.0

# --- Validación de esquemas ---
pydantic>=2.11.0,<3.0.0

# --- ML & Data ---
scikit-learn==1.9.0
scipy>=1.16.3,<1.17.0
joblib>=1.4.0,<2.0.0
pandas>=3.0.5,<4.0.0
numpy>=2.5.2,<3.0.0

# El servicio generativo Legacy_AI no se carga en el MVP desplegado (su router
# está comentado en backend/main.py) y sus SDK quedan fuera del bundle de Vercel.
# Para habilitarlo en local: pip install -r requirements-ai.txt

# ============================================================
# NOTAS DE COMPATIBILIDAD
# ============================================================
# NumPy 2.x tiene breaking changes de ABI respecto a 1.x.
# scikit-learn 1.9 ya compila contra NumPy 2.x sin problema.
# pandas 3.x elimina la indexación encadenada (df[...][...]) —
#   revisar nbo_router.py e inferencia.py por este patrón.
# google-genai usa client = genai.Client() en lugar de
#   genai.configure(api_key=...) — ver MIGRATION_GUIDE.md adjunto.
